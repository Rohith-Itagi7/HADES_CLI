# Open GitHub Issues – PareekshithPalat/HADES_CLI

Total open issues: 20

| # | Title |
|---|-------|
| 28 | **fix(provider):** prevent premature Ollama timeouts during slow local inference |
| 27 | **feat(security):** user‑configurable permission policy engine with dynamic risk modes (/permissions) |
| 25 | **feat(agent):** dynamic Agent Skills engine for extensible workflows and multitasking (~/.hades/skills/) |
| 24 | **feat(mcp):** interactive MCP server connection manager & live key/URL configuration (/mcp add) |
| 23 | **feat(provider):** add native provider adapters for Anthropic Claude, Google Gemini, and Mistral |
| 21 | **feat(tui):** expand Command Palette into a full‑screen overlay with live help & category filtering |
| 17 | **feat(UI):** UI design change required |
| 16 | **feat(core):** add session tagging and metadata annotations in /sessions manager |
| 15 | **feat(tui):** display live estimated token counter in status bar |
| 14 | **feat(cli):** support one‑off non‑interactive prompt execution via `--prompt / -p` flag |
| 13 | **feat(tui):** add configurable mouse wheel scroll sensitivity in config.toml |
| 12 | **feat(storage):** add session cleanup and prune utility for inactive empty sessions |
| 11 | **feat(tools):** add system.gpu hardware diagnostic tool |
| 10 | **feat(commands):** implement `/clear` command to reset active session context |
| 9  | **feat(provider):** measure and display provider API round‑trip latency during verification |
| 8  | **feat(tui):** add `Ctrl+L` shortcut to scroll to conversation bottom and reset viewport |
| 7  | **feat(core):** support shorthand command aliases in command palette (`/m`, `/s`, `/h`, `/t`) |
| 6  | **feat(tui):** support configurable theme color palette in config.toml |
| 5  | **feat(cli):** add shell completion script generator (bash, zsh, fish, PowerShell) |
| 4  | **feat(tools):** add dedicated `filesystem.search` tool for workspace regex/content pattern matching |
